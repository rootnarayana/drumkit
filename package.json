{
  "name": "simple-html-webapp",
  "version": "1.0.0",
  "description": "Simple HTML page hosted on Azure App Service",
  "private": true,
  "scripts": {
    "start": "npx serve ."
  },
  "engines": {
    "node": "18.x"
  }
}
{
  "name": "simple-html-webapp",
  "version": "1.0.0",
  "description": "Simple HTML page hosted on Azure App Service",
  "private": true,
  "scripts": {
    "start": "npx serve ."
  },
  "engines": {
    "node": "18.x"
  }
}
